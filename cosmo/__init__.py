"""Cosmo — self-hosted multi-currency budget tracker.

This package is the v1 rewrite. The flat modules at the repo root
(app.py, database.py, currency_converter.py, merchant_mapper.py)
are the legacy code being incrementally replaced.
"""

__version__ = "0.1.0-dev"
